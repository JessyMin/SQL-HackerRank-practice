#  2018.3.18

# Correct Query
SELECT CITY, CHAR_LENGTH(CITY)
FROM STATION
WHERE CHAR_LENGTH(CITY) = (SELECT MIN(CHAR_LENGTH(CITY)) FROM STATION)
ORDER BY CITY
LIMIT 1;


# Wrong Query
SELECT CITY, CHAR_LENGTH(CITY)
FROM STATION
WHERE CHAR_LENGTH(CITY) = MAX(CHAR_LENGTH(CITY)
ORDER BY CITY
LIMIT 1;

# ERROR 1111 (HY000) at line 1: Invalid use of group function
